public class App2 {
    public static void main(String[] args){
        /*
        1.) If SuperClass does not declare an exception, then the SubClass can only declare unchecked exceptions
            but not the checked exceptions.

        2.) If SuperClass declares an exception, then the SubClass can only
            declare the child exceptions of the exception declared by the SuperClass, but not any other exception.

            Simply put, subclasses can throw fewer checked exceptions than their superclass, but not more.

         */



    }
}
